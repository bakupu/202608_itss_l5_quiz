# workflow

確定した作業手順。検証済みのものだけ置く。

**環境構築・公開・データ生成のコマンドは必ずここへ手順書として残す。**
別PCで `git clone` した直後に、ここを見れば同じ状態を再現できることを保つ。
ターミナルで打って終わりのアドホック実行にしない。

各手順書の冒頭に**検証ステータス**（検証済み/未検証、検証日）を書く。未検証の手順を断定形で書かない。

> 運用方式・命名ルール・カテゴリ定義は [CLAUDE.md](../CLAUDE.md) が真実源。
